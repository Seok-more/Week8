#Echo
make echo_server
make echo_client
./echo_server 12345
./echo_client localhost 12345
telnet localhost 12345

#Tiny
webproxy-lab/tiny$ 여기서
make
./tiny 8080
http://localhost:8080/
(deprecated)http://localhost:8080/cgi-bin/adder?222&111
http://localhost:8080/adder.html

#Proxy
./driver.sh 

# 아 헤드처리 했던걸 없애야하네

ps aux | grep proxy 명령어는 **리눅스/유닉스 터미널에서 "proxy"라는 이름이 포함된 실행 중인 프로세스(즉, 백그라운드에서 동작 중인 프로그램)**를 찾을 때 사용하는 명령어입니다.

ps aux | grep tiny

pkill -f './proxy'
pkill -f './tiny'