11.6-C
Inspect the output from Tiny to determine the version of HTTP your
browser uses.
: Request line: GET / HTTP/1.1 -> 1.1버전 사용

11.7
Extend Tiny so that it serves MPG video files. Check your work using a real
browser.
: tiny.c만 고치는게 아니라 html도 수정해야한다, MPG 안 돼서 MP4로 함
http://localhost:8080/

11.9
Modify Tiny so that when it serves static content, 
it copies the requested file to the connected descriptor 
using malloc, rio_readn, and rio_writen, 
instead of mmap and rio_writen.
:
mmap 대신 malloc/read/write로 바꾸면
더 폭넓은 환경에서 동작하고
메모리 관리가 명확하고
에러/보안 관리도 용이해지고
기본적인 파일 복사 원리를 더 잘 이해할 수 있습니다!

11.10
A. WriteanHTMLformfortheCGIadderfunctioninFigure11.27.Yourform
should include two text boxes that users fill in with the two numbers to be
added together. Your form should request content using the GET method.
B. Check your work by using a real browser to request the form from Tiny,
submit the filled-in form to Tiny, and then display the dynamic content
generated by adder
:
html은 ai한테 만들어 달라고 함 뭔데 이게 
http://localhost:8080/adder.html


11.11
Extend Tiny to support the HTTP HEAD method.Check your work using telnet
as a Webclient.
:
HEAD는 GET과 거의 동일하지만, 서버가 응답할 때 본문(body)을 보내지 않고 헤더(header)만 보내는 요청 방식
클라이언트는 리소스의 상태를 확인할 수 있지만, 실제 콘텐츠를 다운로드하지않음

HTTP/1.0 200 OK
Server: Tiny Web Server
Connection: close
Content-length: 205
Content-type: text/html

Connection closed by foreign host.

[구현] 웹 프록시 (순차적 처리)

[구현] 동시 병렬 처리

[구현] 캐싱 웹 오브젝트

[테스트] PC 네트워크 설정 > 프록시 설정

[테스트] 로컬 > 프록시 > tiny home.html 요청 (성공)

[테스트] 원격 프록시 서버 중지 후, 로컬 > 프록시 > tiny home.html 요청 (실패)

[테스트] 프록시 TC수행점수



pthread_rwlock_t는 POSIX 스레드 라이브러리에서 제공하는 읽기-쓰기 락(Read-Write Lock)
-> 여러 스레드가 공유 데이터를 읽을 때는 동시에 접근 가능. 데이터를 쓰는 스레드는 단독으로 접근해야 함.

여러 읽기(동시가능)
pthread_rwlock_rdlock

쓰기 (삽입/삭제, 단독만 가능)
pthread_rwlock_wrlock

큐를 쓰면 중간에 있는 사용된 노드를 큐의 맨뒤로 옮기는 과정이 굉장히 불편
-> 앞뒤 연결 리스트 쓰자
